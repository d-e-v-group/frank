<article class="case-subintro" <?php echo  ($section_id) ? 'id="'.$section_id.'"' : false ; ?> <?php echo  ($section_id) ? 'data-nav-section="'.$section_id.'"' : false ; ?>>
    <div class="container">
        <div class="grid">
            <div class="col-sm-5 col-sm-offset-1" data-appear-block>
                <?php if(isset($section['header'])): ?>
                    <h2><?php echo  $section['header'] ; ?></h2>
                <?php endif; ?>
            </div>
            <div class="col-sm-5">
                <?php if($section['content']): ?>
                    <?php foreach((array)$section['content'] as $text): ?>
                        <div class="text-row" data-appear-block>
                            <?php if(isset($text['small_header'])): ?>
                                <div class="subtitle"><?php echo $text['small_header']; ?></div>
                            <?php endif; ?>
                            <?php if(isset($text['content'])): ?>
                                <?php echo $text['content']; ?>
                            <?php endif; ?>
                        </div>
                    <?php endforeach; ?>
                <?php endif; ?>
            </div>
        </div>
    </div>
</article>
