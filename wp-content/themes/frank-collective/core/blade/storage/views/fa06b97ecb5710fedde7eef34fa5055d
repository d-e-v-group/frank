<article class="case-subintro" <?php echo  isset($section_id) ? 'id="'.$section_id.'"' : false ; ?> <?php echo  isset($section_id) ? 'data-nav-section="'.$section_id.'"' : false ; ?>>
    <div class="container">
        <div class="grid">
            <div class="col-sm-5 col-sm-offset-1" data-appear-block>
                <?php if(isset($h2)): ?>
                <h2><?php echo  $h2 ; ?></h2>
                <?php endif; ?>
            </div>
            <div class="col-sm-5">

                <?php foreach($textrow as $text): ?>
                    <div class="text-row" data-appear-block>
                        <?php if(isset($text['subtitle'])): ?>
                        <div class="subtitle"><?php echo $text['subtitle']; ?></div>
                        <?php endif; ?>
                        <?php if(isset($text['content'])): ?>
                            <?php echo $text['content']; ?>
                        <?php endif; ?>
                    </div>
                <?php endforeach; ?>
            </div>
        </div>
    </div>
</article>